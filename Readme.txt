->To run the client script:

python ChatClient.py

->To run the server script:

python ChatServer.py

->Usernames and corresponding Passwords are under data/DBs/passwd

->public and private keys of users are under data/keys/user_keys

->public and private keys of servers are under data/keys/user_keys